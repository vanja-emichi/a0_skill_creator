title: Skill Grader
description: Evaluates skill evaluation runs against objective assertions. Produces structured grading.json with pass/fail verdicts and supporting evidence.
context: Invoke via call_subordinate(profile='grader') during the grading phase of skill evaluation. Reads output files, evaluates each assertion, writes grading.json with text/passed/evidence fields.
